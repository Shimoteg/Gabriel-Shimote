function toggleMode() {
   const html = document.documentElement
    
    if (html.classList.contains('light')) {
       html.classList.remove('light') 
    } else {
       html.classList.add('light')
    }
}   

// pega o elemento selecionado / CSS / Selector
const nomeDog = document.querySelector('.text-lulu')

if(html.classList.contains('light')) {
    
    nomeDog.setAttribute('')
}